**Take 115 — the audit you ordered. Install this one, not 114.**

Signed release build. Confirm **Tools → Diagnostics → About this build says
take 115** after the GitHub build.

### What the audit found and fixed

Everything T1–T4 was verified INSIDE the sealed zip — 16 of 18 mechanisms clean
on first pass, and the two misses were real:

- The **activity filter still lit up full orange** when active — the same
  loudness the basemap button was cured of. Quieted, and the app now has a
  standing check that counts accent-coloured surfaces and fails above one per
  screen, so this rule can never quietly decay again.
- **Coordinates now truly end the details card** — they had landed above the
  elevation row instead of below it.
- The **error screen** you should never see now uses the same typeface as
  everything else — it had been hiding in a split string.

### The records audit

All 193 landmines verified: complete, no duplicates, none contradicted by the
redesign, 49 of them backed by live automated guards. Two entries were filed
out of numeric order — refiled, and the build now checks ordering forever.

### Otherwise identical to 114

Everything from the visual overhaul (T1–T4) plus the parked Michigan prep.
Same test script as 114: About says 115, then judge it against onX and
AllTrails on your own ground. onX Backcountry screenshots welcome whenever —
the study has a slot waiting for them, and Michigan runs the moment your
visual verdict is in.
